--- src/App.tsx
+++ src/App.tsx
@@ -11,7 +11,7 @@
 } from './types';
 import { parseSrt, generateFilename } from './utils/srtParser';
 import { urlToPngBlob, downloadBlob } from './utils/imageExporter';
-import { getDbItem, setDbItem } from './utils/db';
+import { getDbItem, setDbItem, clearDb } from './utils/db';
 import { SAMPLE_SRT_PRESETS } from './data/sampleSrt';
 import { Header } from './components/Header';
 import { StylecardSection } from './components/StylecardSection';
@@ -511,16 +511,7 @@
   const handleClearData = async () => {
     if (confirm('Tem certeza que deseja limpar TODOS os dados e começar de novo?')) {
       // Clear IndexedDB items
-      await setDbItem('rawSrtText', '');
-      await setDbItem('entityRegistry', null);
-      await setDbItem('entityReferenceSheets', {});
-      await setDbItem('frames', []);
-      await setDbItem('stylecard', {
-        id: '1',
-        name: 'Empty',
-        textStyle: '',
-        aspectRatio: '16:9'
-      });
+      await clearDb();
 
       // Force a full page reload to ensure all async tasks stop and state is pristine
       window.location.reload();
